From 5a121f27e1efd76c809b92dbb2d3a587d0af389c Mon Sep 17 00:00:00 2001
From: Boris Belyaev <bbor1974@gmail.com>
Date: Tue, 8 Sep 2026 14:06:16 +0400
Subject: [PATCH 2/2] init: Add get_kernel_cmdline_partitions

Change-Id: I2e1a2992cc8b6470aa2cb52d5edcdb3baaafac22
---
 init/init.cpp |  2 +-
 init/util.cpp | 71 +++++++++++++++++++++++++++++++++++++++++++++++++++
 init/util.h   |  1 +
 3 files changed, 73 insertions(+), 1 deletion(-)

diff --git a/init/init.cpp b/init/init.cpp
index 5ba7eec..483afb2 100644
--- a/init/init.cpp
+++ b/init/init.cpp
@@ -683,7 +683,7 @@ int main(int argc, char** argv) {
     }
 
     signal_handler_init();
-
+    get_kernel_cmdline_partitions();
     property_load_boot_defaults();
     export_oem_lock_status();
     start_property_service();
diff --git a/init/util.cpp b/init/util.cpp
index 89d3276..8be63e5 100644
--- a/init/util.cpp
+++ b/init/util.cpp
@@ -577,3 +577,74 @@ bool expand_props(const std::string& src, std::string* dst) {
 
     return true;
 }
+
+void get_kernel_cmdline_partitions(void)
+{
+	char cmdline[1024], partition_dev[64], partition_name[64];
+	char *ptr,*end;
+	int fd, len;
+	char dev[64], partition[64];
+
+	mkdir("/dev/block", 0755);
+	mkdir("/dev/block/by-name", 0755);
+
+	fd = open("/proc/cmdline", O_RDONLY);
+	if (fd >= 0) {
+		int n = read(fd, cmdline, 1023);
+		if (n < 0) n = 0;
+
+		/* get rid of trailing newline, it happens */
+		if (n > 0 && cmdline[n-1] == '\n') n--;
+
+		cmdline[n] = 0;
+		close(fd);
+	} else {
+		cmdline[0] = 0;
+	}
+	//LOG(ERROR)("get_kernel_cmdline_partitions! %s\n",cmdline);
+	ptr = strstr(cmdline, "partitions=");
+	if (ptr == NULL) {
+		//ERROR(">> cmdline not contain partition info!\n");
+		return;
+	}
+	ptr += strlen("partitions=");
+        
+        end = strstr(ptr," ");
+        if(end>0)
+          *end = 0;
+        //ERROR("ptr = %s",ptr);
+	memset(dev, 0, sizeof(dev));
+	memset(partition, 0, sizeof(partition));
+	memset(partition_dev, 0, sizeof(partition_dev));
+	memset(partition_name, 0, sizeof(partition_name));
+
+	while (ptr && *ptr!=0) {
+		char *x = strchr(ptr, '@');
+		len = x - ptr;
+		memccpy(partition_name, ptr, '@', len);
+		ptr = x + 1;
+		x = strchr(ptr, ':');
+		if(x == NULL) {
+			len = strlen(ptr);
+		} else {
+			len = x - ptr;
+		}
+
+		memccpy(partition_dev, ptr, ':', len);
+		snprintf(partition, sizeof(partition), "/dev/block/by-name/%s", partition_name);
+		snprintf(dev, sizeof(dev), "/dev/block/%s", partition_dev);
+		
+		//ERROR("dev = %s partition = %s\n",dev,partition);
+		symlink(dev, partition);
+		memset(dev, 0, sizeof(dev));
+		memset(partition, 0, sizeof(partition));
+		memset(partition_dev, 0, sizeof(partition_dev));
+		memset(partition_name, 0, sizeof(partition_name));
+
+		if(x == NULL)
+			break;
+		else
+			ptr = x + 1;
+	}
+}
+
diff --git a/init/util.h b/init/util.h
index af4b098..9163721 100644
--- a/init/util.h
+++ b/init/util.h
@@ -67,4 +67,5 @@ int restorecon_recursive_skipce(const char *pathname);
 std::string bytes_to_hex(const uint8_t *bytes, size_t bytes_len);
 bool is_dir(const char* pathname);
 bool expand_props(const std::string& src, std::string* dst);
+void get_kernel_cmdline_partitions(void);
 #endif
-- 
2.43.0

